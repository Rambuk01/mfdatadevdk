{% extends "base.twig" %}

{% block content %}
<section class="section">
    <div class="container container-narrow" style="text-align: center;">
        <h1>404</h1>
        <p class="hero-subtitle">{{ t.error.not_found }}</p>
        <a href="{{ lang == 'da' ? '/' : '/en/' }}" class="btn btn-primary">{{ t.error.go_home }}</a>
    </div>
</section>
{% endblock %}
